import { Screen } from '../components/Screen';

/** S4 — Search results via Google Books (built in A4). */
export default function Search() {
  return <Screen title="Search" spec="S4 · Google Books search — brief A4" />;
}
