import { Screen } from '../../components/Screen';

/** S8 — Notifications inbox (built in A7). */
export default function Inbox() {
  return <Screen title="Inbox" spec="S8 · rec received / started / finished pings — brief A7" />;
}
