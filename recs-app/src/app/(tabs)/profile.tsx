import { Screen } from '../../components/Screen';

/** S10 — Minimal profile/settings. */
export default function Profile() {
  return <Screen title="Me" spec="S10 · display name, avatar, sign out" />;
}
